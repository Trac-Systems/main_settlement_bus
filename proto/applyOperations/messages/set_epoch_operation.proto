syntax = "proto3";

package apply.operations;

message SetEpochOperation {
    bytes          pd  = 1; // proof data that actually contains challenge + proof + sig of the proposer
    repeated bytes app = 2; // Array of approvals that signed proof data
}
