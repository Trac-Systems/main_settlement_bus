syntax = "proto3";

package apply.operations;

message SetEpochOperation {
    uint64         pd  = 1; // proof data
    bytes          rs  = 2; // Requester sig
    repeated bytes app = 3; // Array of approvals
}
